---
---

Add `docs/mini-native-cross-platform.md` — a design note on making `@amritk/mini-native`'s web output a production target rather than a preview. Covers the semantics layer that closes the web and native accessibility gaps together, host-normalised event payloads, the flow wrapper's accessibility hazard, a `/ui` component and typography layer with a role-based `as` override, the platform-divergence seams, environment signals, prior art, an HTML-first alternative worked through in full, the design's costs, a strategy for a codebase starting fresh, and why style is the harder half. Documentation only; no published package changes.
