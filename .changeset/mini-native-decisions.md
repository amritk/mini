---
---

Record the settled cross-platform decisions where they bind: compiler ceilings per package (diagnostics for `@amritk/mini`, an optional optimising plugin for `@amritk/mini-native`, neither ever required for correctness) in both `AGENTS.md` files and `.claude/architecture.md`, and the flow-wrapper accessibility invariant in `mini-native`'s `Host` contract. Documentation and comments only; no runtime behaviour changes.
