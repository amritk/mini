android.useAndroidX=true
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=768m
# The compile check has one module and runs once in CI; a daemon would outlive
# the job and hold memory a runner needs for the rest of the build.
org.gradle.daemon=false
