/**
 * @packageDocumentation
 *
 * The rspeedy build for a `@amritk/mini-lynx` app: two chunks, one template,
 * and the dev server Lynx Explorer scans.
 */

export type { PluginMiniLynxOptions } from './plugin'
export { pluginMiniLynx } from './plugin'
